# Decision Log

Keep this file short. Record only decisions that change product behavior, architecture, integration, scope, or long-term maintenance.

## Current phase

- Completed phases: environment, architecture, and deterministic greybox.
- Completed on: 2026-08-15.
- Evidence:
  - Git repository initialized on `main`;
  - source-of-truth documents and lean agent instructions added;
  - 360 × 640 Phaser canvas and interactive greybox added;
  - pure run-state transitions and a deterministic varied traffic schedule added;
  - official BeautyBomb homepage, brand page, and Waterbomb product page reviewed on 2026-08-15;
  - Pixel Art Bible and batched Asset Manifest added;
  - TypeScript check passed;
  - 17 domain and configuration tests passed;
  - Vite production build passed;
  - npm audit reported 0 vulnerabilities;
  - no hooks, lint stack, UI framework, backend, or agent packs installed.
- Current phase: the corrected scale gate, clean courier static master and drive sheet, the first obstacle trio's static masters and drive sheets, the gap-free coherent neighborhood-city environment v8 runtime, the white-curb road v6 runtime, the branded HUD v1 candidate, and the approved in-scene intro flow are integrated. The next static approval gate is the `OBS-004` small delivery van.
- The in-app browser smoke tests on 2026-08-16 confirmed that the enlarged courier remains inside the canvas and readable on all three lanes, and that the corrected near-lane convoy keeps visible space between both cars. Full-route feel remains an owner playtest gate.

## Accepted decisions

| ID | Decision | Reason |
|---|---|---|
| D-001 | Position the work as an independent concept. | Avoids inventing a client relationship or commercial result. |
| D-002 | Use a portrait 360 × 640 viewport and exactly three depth lanes. | Matches the selected mobile-first gameplay concept and keeps input readable. |
| D-003 | Use TypeScript, Vite, Phaser 3.90, and Vitest. | Mature 2D tooling with a small operational surface and testable domain logic. |
| D-004 | Keep rules outside Phaser scenes. | Prevents rendering code from becoming the only source of gameplay truth. |
| D-005 | Prefer iframe integration for a real client site. | Isolates styles, dependencies, sizing, and runtime failures. |
| D-006 | Demo rewards are local and fictional; production rewards are server-authoritative. | Prevents client-side prize tampering and false promotion claims. |
| D-007 | Use the selected concept with a turquoise vehicle, horizontal Water Bomb tube, and pixel BeautyBomb logo. | Captures the approved visual direction and later correction. |
| D-008 | Do not copy the portfolio project's large skill collection or automatic design hooks. | They add context and maintenance cost before this game needs them. |
| D-009 | Adapt Karpathy's baseline/measure/keep-discard loop without an infinite run or destructive reset. | Preserves measurable iteration while remaining safe for a product repository. |
| D-010 | Use a fixed 45-second greybox route with a varied deterministic traffic schedule. | Keeps difficulty reproducible while mixing singles, parallel and staggered pairs, convoys, and scattered groups without an instant three-lane wall. |
| D-011 | Extend stable BeautyBomb site traits through an original pixel-art system rather than copying seasonal campaign artwork. | Keeps the concept recognizably aligned with the brand while avoiding dependence on temporary collaborations and unlicensed page art. |
| D-012 | Approve one static courier hero pose before producing animations or the full atlas. | Locks perspective, silhouette, product placement, and logo simplification before expensive downstream asset work. |
| D-013 | Approve `veh-001-courier-near-concept-v1.png` as the courier design master. | The owner accepted the light-blue van, right-facing perspective, horizontal Waterbomb tube, and pixel BeautyBomb wordmark on 2026-08-15. |
| D-014 | Replace the front-heavy `v1` perspective with the side-profile `veh-001-courier-side-concept-v2.png`. | A horizontal left-to-right runner needs the long side plane to dominate; the new candidate uses an `85–90%` side view and only a narrow front plane. |
| D-015 | Approve `veh-001-courier-side-tapered-tube-concept-v3.png` as the courier design master. | The owner accepted the side-profile van and corrected tube taper on 2026-08-15; runtime production can use this version as its visual source. |
| D-016 | Limit each traffic formation to two vehicles and require an open lane within every `1,200 ms` spawn window. | Prevents the scattered three-car sequence and neighboring groups from creating an unfair full-road trap. |
| D-017 | Use a pink hatchback, yellow sedan, and green boxy wagon as the first playable traffic trio. | Distinct color, roofline, and wheelbase make obstacle recognition faster without competing with the turquoise courier. |
| D-018 | Use shared lane scale factors `0.88`, `0.94`, and `1.00` for vehicle visuals and collision rectangles. | Keeps the shallow 2.5D depth cue readable without allowing visible art and gameplay hitboxes to drift apart. |
| D-019 | Supersede D-018 with wheel baselines `350/424/508`, visual scales `1.12/1.22/1.32`, and collision scale ratio `0.84`; enlarge the green wagon by an additional `1.18`. | The first integrated scale left roughly half of each road section visually empty and reduced sprite legibility. Separating art scale from collider scale improves composition without materially increasing difficulty. |
| D-020 | Increase the same-lane convoy interval from `650 ms` to `750 ms` and require at least `16 px` of visible clearance. | At the near-lane scale, the enlarged green wagon and following pink hatchback overlapped by about `0.7 px`; the new interval restores a readable gap without changing group timing or route difficulty. |
| D-021 | Supersede the courier branding details in D-007 and the design master in D-015 with the floral `v4` redesign and native `v2` candidate. | The side panel now uses two ordered flower decals; the clean roof tube has no print or intermediate neck and widens more strongly away from the cap; triangular wheel highlights provide readable rotation phases. Canvas, origin, and body collider remain unchanged. |
| D-022 | Supersede the floral details in D-021 with the clean `v5` concept and clean native `v2` candidate. | The owner removed all side-panel decoration and requested a tube contour measured from the product reference: direct cap seam, no print, and a continuous gradual taper without a sudden shoulder. Wheel phase highlights, canvas, origin, and collider remain unchanged. |
| D-023 | Replace the manual `104 × 80` courier redraw pipeline with a deterministic master-derived `208 × 160` export rendered at `0.5` base scale. | The former script edited the old low-resolution `v1` sprite instead of exporting the approved master, reducing 65,565 master colors to 38 and changing geometry before Phaser loaded it. The `v3` export preserves master pixels through one resize while keeping the same world size and collision behavior. |
| D-024 | Make approved-master fidelity a repository-wide asset architecture invariant. | Prevents future sprites from being regenerated, redrawn from previews, repeatedly resized, or integrated without metadata and an in-game static comparison gate. |
| D-025 | Supersede the unmarked `v5` courier candidate with the owner-requested Waterbomb-brand `v6` alpha master and `v4` runtime export. | The new master follows the supplied face-cream reference with a broad ribbed white cap, full-width turquoise tube, and reference-derived package hierarchy; it permits only the two-line acid-yellow/deep-violet `BEAUTY BOMB` side treatment. The deterministic export, `208 × 160` canvas, `0.5` base scale, origin, wheel baseline, and body-only collider remain unchanged. |
| D-026 | Approve the `v6` Waterbomb-brand master and static `v4` export; derive and integrate the four-frame `v5` drive sheet. | The owner approved the static design on 2026-08-20. Every drive frame is built from the immutable master at high resolution, then receives exactly one nearest-neighbor export. Frame zero must remain byte-identical to v4; all frames share the `208 × 160` canvas, origin, wheel baseline, and body-only collider. |
| D-027 | Approve the `v5` courier drive cycle without an additional visual review. | The owner explicitly accepted the integrated four-frame loop on 2026-08-20. Its approved runtime remains deterministically derived from the immutable `v6` master; any visual change now requires a new versioned master and decision. |
| D-028 | Approve the v2 pink hatchback, yellow sedan, and green wagon masters; derive and integrate their static exports and drive candidates. | The owner approved all three separate v2 traffic masters on 2026-08-20. Each static export removes only source alpha noise below 16, crops transparent padding, and performs one nearest-neighbor resize. Each four-frame drive candidate derives from that approved master, preserves canvas/origin/baseline/body collider, and keeps its zero frame byte-identical to the static runtime. |
| D-029 | Approve the v2 obstacle drive cycles. | The owner accepted the three four-frame loops on 2026-08-20. Their runtime sheets are immutable, deterministically derived from the approved masters, and retain the static zero frame, canvas, origin, wheel baseline, and body-only collider. Any visual change now requires a new versioned master and decision. |
| D-030 | Approve the `ENV-SCENE-001 v2` still master and integrate the first deterministic parallax set. | The owner accepted `visual-references/env-001-gameplay-still-concept-v2.png` on 2026-08-20. `scripts/build_environment_parallax_v1.py` locks its hash and derives `ENV-001–004`, `ENV-006`, and `ENV-008` through one nearest-neighbor resize per source crop. The scene scrolls those layers at low distinct speeds while keeping the approved road/lane positions, gameplay rules, and collision geometry unchanged. |
| D-031 | Replace environment v1 runtime tiles with deterministic seam-safe POT v2 cycles and make parallax layering explicit. | `scripts/build_environment_parallax_v2.py` derives each useful strip from the immutable environment master through one nearest-neighbor resize, then appends its horizontal mirror without generating pixels. The resulting POT textures have exact cyclic edges and documented periods. A pure motion helper distinguishes `route-loop` modulo from `arrival-finite` clamp; named depth bands keep HUD and controls above every parallax layer, and reduced-motion freezes decorative parallax and camera shake. A full 45-second browser run reached `delivered` with no console errors. |
| D-032 | Approve `ENV-SCENE-001 v3` and replace mirrored landmark cycles with direct panoramas protected by neutral edge gutters; increase the base route speed to `72 px/s`. | The owner approved `visual-references/env-001-parallax-seamless-v3.png` on 2026-08-21. Its outer safe zones contain no shops, trees, lamps, or facades. `scripts/build_environment_parallax_v3.py` creates POT textures from direct panorama strips and mirrors only a `128 px` neutral source gutter, so no foreground landmark is duplicated at a cycle edge. Road bands, collision geometry, HUD depth, reduced-motion behavior, and finite arrival behavior remain unchanged. |
| D-033 | Approve and integrate the coherent `ENV-SCENE-001 v4` A/B/C panorama using shared-coordinate geometric masks. | The owner approved `visual-references/env-001-parallax-coherent-v4-candidate.png` on 2026-08-21. `scripts/build_environment_parallax_v4.py` replaces `remove-cyan-sky` with continuous far/mid/near polygon profiles and a `128 px` occlusion reserve, so cyan facades remain intact and independently moving layers cannot expose architectural gaps. `ENV-001–004` and `ENV-006` use the same `A→B→C→A` coordinates and direct POT loops; the road remains `72 px/s`, while HUD depth, collisions, reduced motion, and arrival-finite behavior remain unchanged. |
| D-034 | Supersede the split-city implementation in D-033 with one coherent city layer and restore the approved v3 road geometry. | The independent far/mid/near offsets visibly displaced connected towers, facades, trees, lamps, and sidewalks, while the v4 road crop compressed the playable road. `ENV-004` now carries the complete v4 skyline-to-sidewalk composition at one speed; `ENV-002` and `ENV-003` have no standalone runtime motion. `ENV-006` again derives from the immutable v3 road master with the exact `y=282–522` band. Sky, neutral foreground, HUD depth, `72 px/s` road speed, collisions, gameplay rules, reduced motion, and arrival-finite behavior remain unchanged. |
| D-035 | Integrate the branded arcade HUD v1 as a review candidate without changing gameplay geometry. | `scripts/build_hud_ui_v1.py` authors full/empty sticker hearts, the `324 × 16` progress frame, four `76 × 48` up/down button states, and the fixed `360 × 118` control console directly on the runtime pixel grid. The `LIVES` and `MOVE` labels are removed as requested. Life authority, progress calculation, timer, control centers, hit areas, HUD depth, overlays, input handling, and the `360 × 640` layout remain unchanged. The candidate is not immutable until owner approval. |
| D-036 | Replace the defective v4 city polygon with a boundary-connected v5 matte and replace the gameplay title text with `UI-009`. | The v4 profile occasionally traced cloud bottoms and filled those columns down to the sidewalk, making opaque sky-colored towers cross the independently moving clouds. The v5 builder removes only adaptive sky pixels connected to the master boundary, retains the single city-anchor component, and rejects detached clouds; the immutable v4 master, unified city motion, road, foreground, speeds, and gameplay geometry remain unchanged. `UI-009` is authored at `242 × 28` with zero resize or antialiasing and replaces only the old Phaser title object at `x=180, y=32`. |
| D-037 | Remove the visible seconds countdown from the gameplay HUD. | The progress bar now carries all in-run progress communication. The deterministic timer remains active for run duration, spawning, delivery, and failure authority, but no seconds or phase-status text is rendered above the bar. |
| D-038 | Supersede the branded courier visuals in D-025–D-027 with the clean `v7` master, static v5, and drive v6. | The owner requested no lettering or decoration on the van body and no text, logos, icons, or printed graphics on the roof tube. The new version preserves the `208 × 160` runtime canvas, `0.5` base scale, lane scales, origin, wheel baselines, body-only collider, four-frame motion contract, and traffic behavior. |
| D-039 | Add the `UI-010` top-right pause control and an in-canvas continue/restart dialog. | The `32 × 32` two-frame button matches the existing yellow navigation controls and sits at `x=332, y=32` with safe title and edge spacing. Pausing freezes route authority and all visible motion without changing the run state; continue resumes the exact run, while restart resets the deterministic route. |
| D-040 | Strengthen deterministic traffic spacing from convoy-only `16 px` clearance to every chronological same-lane pair with `48 px` clearance, and disallow delayed cross-lane traps. | The real approved vehicle bounds showed that a `750 ms` convoy could leave only `16.54 px` in the near lane, while `480/780 ms` cross-lane offsets hid a second obstacle during the decision window. Templates now use the minimum `933 ms` convoy interval, `≤200 ms` visible formations, or a full `1,200 ms` separation. |
| D-041 | Replace only the v5 coherent city with the approved v6 neighborhood panorama while preserving the sky, road, foreground, and motion contract. | The previous facades repeated one structure, contained malformed windows and upper-floor details, and placed a dense high-rise wall directly behind the storefronts. The v6 RGBA master uses varied readable street fronts, medium urban depth, a restrained skyline, and a complete edge-to-edge sidewalk composition. `ENV-004` remains one layer at `0.56`; unchanged `ENV-001`, `ENV-006`, and `ENV-008` hashes prove the road geometry and other separated layers were not modified. |
| D-042 | Correct the v6 city export crop with a facade-safe v7 runtime while keeping the approved v6 master immutable. | The v6 source box ended at `x=1907`, which cut the terminal beige building in half at the cyclic join. Extending the export boundary to `x=2023` includes the complete building and sidewalk before returning to the complete first facade. No regeneration was required; all other layer hashes, movement values, depths, and gameplay geometry remain unchanged. |
| D-043 | Remove the residual cyan street-level interval from the facade-safe join with a v8 runtime crop. | After restoring the complete beige facade, the unused sidewalk padding at both panorama ends combined into a visible cyan gap. Cropping the loop period to `[123, 0, 2005, 693]` joins the two complete facade boundaries and their sidewalks directly. The approved master, other city content, sky, road, foreground, movement values, depths, and gameplay geometry remain unchanged. |
| D-044 | Replace the bright-green upper and lower road curbs with neutral white pixel shading in `ENV-006 road v6`. | The green edge color competed with the game UI and no longer matched the requested road treatment. A deterministic row-limited transform changes only curb rows `0–5` and `388–397`; the approved master, asphalt, lane markings, road band, speed, scale, cyclic construction, collisions, city, sky, and foreground remain unchanged. |
| D-045 | Enlarge the mobile lane controls to `1.5×` while preserving a clear lower-panel margin. | The native `76 × 48` frames remain unchanged, render as `114 × 72` touch targets, and move to centers `111/249, 572`. This gives a materially larger tap target, a `24 px` inter-button gap, and `32 px` of space below the buttons without changing keyboard input, lane authority, or panel geometry. |
| D-046 | Add symmetric exit/pause utility controls, an attached sound placeholder, and a lowered title without an outer game frame. | `UI-011` mirrors the `32 × 32` pause control at `x=28, y=32`; `UI-012` stays inside the lower panel at `x=346, y=540`; both expose only pressed-state feedback until their future host/audio contracts exist. Moving `UI-009` to `y=38` separates the title from the utility-button centerline; the game canvas remains visually unframed. |
| D-047 | Replace the former START popup with the approved in-scene `UI-013`/`UI-014` instruction and a guarded one-second intro transition. | The existing `ready` phase now freezes route authority, parallax, traffic, and drive frames while showing the live scene, inert lane controls, a subtle courier idle bob, and a pulsing `ЖМИ`. A first canvas press, `Enter`, or `Space` is consumed by one `1,000 ms` color transition; only completion calls `startRun`, exposes utility controls, and restores gameplay. Reduced motion removes spatial intro movement. The font is rasterized locally at build time, so no new domain state, scene, runtime dependency, or font CDN is required. |
| D-048 | Keep the approved `ЖМИ` scale pulse and replace the single-resolution presentation path with a `2×` backing canvas plus selective filtering. | The defect came from applying fractional `1.00 → 1.06` scaling to a low-resolution texture under `NEAREST`, followed by another whole-canvas pixelated scale. The logical `360 × 640` scene and all gameplay coordinates remain unchanged, while Phaser renders `720 × 1280`; authored pixel textures stay `NEAREST`, and only fractionally scaled `UI-014` uses `LINEAR`. Browser checks confirmed a complete aligned intro and gameplay frame, unchanged one-second pulse/transition behavior, a `720 × 1280` canvas presented at `360 × 640`, and no runtime overlay. |
| D-049 | Animate the courier on the intro screen with the approved driving cadence while freezing wheel rotation. | A new `VEH-001-INTRO` sheet is derived directly from the clean `v7` master. It applies the same body and tube offsets as the integrated drive sheet at `9 fps`, but composites both hubs from the static source for every frame. The ready scene plays this sheet; ordinary gameplay switches back to the unchanged `v6` drive sheet at the existing transition boundary. |
| D-050 | Temporarily remove the pause, exit, and sound buttons from the rendered game UI. | `UI-010` through `UI-012` stay in the repository as source candidates, but the scene no longer loads, creates, or displays them. Local pause remains available through `P` and `Esc`, including the existing continue/restart dialog. |

## Open production questions

- Official brand-book and asset permission.
- Client website stack and iframe policy.
- Production campaign rules, legal copy, geography, dates, and inventory.
- Prize API, idempotency, rate limits, and fraud controls.
- Analytics provider and consent requirements.
